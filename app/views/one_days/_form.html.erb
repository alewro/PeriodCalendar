<%= form_with(model: one_day) do |form| %>
  <% if one_day.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(one_day.errors.count, "error") %> prohibited this one_day from being saved:</h2>

      <ul>
        <% one_day.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :start_time, style: "display: block" %>
   <span class="date-time"><%= form.date_select(:start_time, default: Date.today) %></span>
  </div>

  <div>
    <%= form.submit(@one_day.new_record? ? "Add sex" : "Update sex", class:"submit-btn") %>
  </div>
<% end %>
